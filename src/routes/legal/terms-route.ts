/**
 * Terms of Service page. Public route (`data: { auth: false }`) reachable by
 * guests and store reviewers. All content is rendered by the shared
 * `<legal-document>` element from the `legal.terms.*` i18n namespace.
 */
export class TermsRoute {}
