/**
 * Privacy Policy page. Public route (`data: { auth: false }`) — this URL is the
 * one registered as the App Store Connect / Google Play Privacy Policy URL, so
 * it must render without prior in-app navigation. Content is rendered by the
 * shared `<legal-document>` element from the `legal.privacy.*` i18n namespace.
 */
export class PrivacyRoute {}
